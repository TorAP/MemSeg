observation_budget: 10
method: bayes
metric:
name: eval_AUROC-pixel
goal: maximize
parameters:
dataset:
    category: capsule
    image_size:
    values: [128, 256]
  model:
    backbone:
    values: [resnet18, wide_resnet50_2]
